extends base

append styles
  link(href='/chatphrase.css', rel='stylesheet', type='text/css')

append scripts
  script(src='/adapter.js')

block body
  #landing.page.active
    .centercell
      h1.sitename chatphrase
      form(onsubmit="goToPhrase(document.getElementById('phrase').value);return false;")
        input#phrase(type='text')
        button(type="submit") Go
  #limbo.page.inactive
    #virgil
  #room.page.inactive
    #waiting
    video(autoplay)#vidscreen
    video(autoplay,muted)#pip
  script(src='/chatphrase.js')
